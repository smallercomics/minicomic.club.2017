<div class="previous-contributor">
    <a href="{{link}}"><img class="profile "src="./{{ asset image }}" /></a>
    <div class="info">
        <h2>{{title}}</h2>

        <h3><a href="{{link}}">{{  name  }}</a></h3>
    </div>
    
</div>